spring.datasource.driverClassName = com.mysql.jdbc.Driver
spring.datasource.url = jdbc:mysql://localhost:3306/spring-boot-druid?useUnicode=true&characterEncoding=utf-8
spring.datasource.username = root
spring.datasource.password = root
#  别名扫描pojo,
mybatis.type-aliases-package=com.yuan.ngu.springibatis

#mapper xml的位置
mybatis.mapper-locations=classpath:mapper/*Mapper.xml
